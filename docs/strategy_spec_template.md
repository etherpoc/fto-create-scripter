# Strategy Spec: <戦略名>

このテンプレを `strategies/<name>/spec.md` にコピーして埋める。
**曖昧な点は仮定で埋めずに必ず質問する**。曖昧な仕様で実装すると、
あとで「想定と違う」が必ず出る。

---

## 概要
- 戦略の一行サマリ:
- 着想元 / 出典（任意）:

## 対象
- 通貨ペア:
- 主時間足:
- 上位足 (MTF):
  - 使うか / 使わないか:
  - 使う場合の時間足:
  - 参照の仕方（確定足のみ / 値の使い方）:

## ロングエントリー条件
箇条書きで、判定に使う指標と閾値を全部書く。

- 条件 1:
- 条件 2:
- すべて AND / OR の関係:

## ショートエントリー条件
- 条件 1:
- 条件 2:
- すべて AND / OR の関係:

## エグジット条件
- 利確 (TP):
  - 例: `+ATR × 3.0`、固定 50 pips、上位足の構造ターゲット 等
- 損切り (SL):
  - 例: `−ATR × 2.0`、直近スイング下、固定 30 pips 等
- 時間ベース:
  - 例: N 本経過で強制クローズ
- 反対シグナルでクローズ / ドテン:
  - する / しない、ドテンするならどちらが優先

## リスク / 数量
- 1 トレードあたりロット:
- 同時保有可能ポジション数:
- ドテンの可否（反対方向シグナルで既存ポジを閉じて反対側へ入るか）:
- 同方向の追加発注:

## 最適化パラメータ
| パラメータ名 | 既定値 | 探索範囲 | 備考 |
|---|---|---|---|
|  |  |  |  |
|  |  |  |  |

## 検証スコープ
- バックテスト期間:
- 想定スプレッド / 手数料:
- 通貨換算（pip_value 等）の仮定:
- 既知のリスク・除外条件（重要指標時は休む、等）:

## FTO へ持って行くとき特に注意したい点
- データ参照で気をつけたいこと（例: 上位足の確定タイミング）:
- 発注 / SL/TP の単位:
- その他:
